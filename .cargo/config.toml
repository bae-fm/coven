# rand 0.9 / getrandom 0.3 need the browser crypto RNG backend on wasm, selected
# by this cfg (the `wasm_js` feature alone is not enough).
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
